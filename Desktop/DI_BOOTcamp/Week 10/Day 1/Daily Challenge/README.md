# Book List App

A simple React + TypeScript app that uses a generic `List` component to render books and lets users add new entries dynamically.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
